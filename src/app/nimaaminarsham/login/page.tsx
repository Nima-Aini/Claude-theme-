"use client";
import { useState } from "react";
export default function AdminLoginPage() {
 const [username,setUsername]=useState(""); const [password,setPassword]=useState(""); const [error,setError]=useState(""); const [loading,setLoading]=useState(false);
 const submit=async(e:React.FormEvent)=>{e.preventDefault();setLoading(true);setError("");try{const r=await fetch("/api/auth/admin",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username,password}),credentials:"include"});const d=await r.json();if(d.success)window.location.href="/nimaaminarsham";else setError(d.error||"خطا در ورود");}catch{setError("خطای ارتباط");}finally{setLoading(false)}};
 return <main className="min-h-screen flex items-center justify-center p-5 bg-[#37474F]" dir="rtl"><form onSubmit={submit} className="relative bg-white rounded-3xl p-8 w-full max-w-sm shadow-2xl"><div className="text-center mb-8"><div className="w-14 h-14 rounded-2xl mx-auto mb-4 flex items-center justify-center bg-[#FF174410] text-[#FF1744] font-black">AK</div><h1 className="text-xl font-black text-gray-800">پنل مدیریت</h1><p className="text-xs text-gray-400 mt-1">NIMA AMIN ARSHAM</p></div>{error&&<div className="bg-red-50 text-red-500 p-3 rounded-xl text-xs text-center mb-4 font-bold">{error}</div>}<div className="space-y-3"><input value={username} onChange={e=>setUsername(e.target.value)} placeholder="نام کاربری" className="w-full px-4 py-3.5 rounded-xl bg-gray-50 border border-gray-100 text-sm" dir="ltr"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="رمز عبور" className="w-full px-4 py-3.5 rounded-xl bg-gray-50 border border-gray-100 text-sm" dir="ltr"/><button disabled={loading} className="w-full py-3.5 rounded-xl bg-[#FF1744] text-white font-bold text-sm disabled:opacity-50">{loading?"لطفا صبر کنید...":"ورود"}</button></div></form></main>;
}
