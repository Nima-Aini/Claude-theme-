// Production credentials are never seeded from source code.
// Create the first administrator with `npm run admin:create` and environment
// variables documented in README.md.
console.log("No default accounts were created.");
